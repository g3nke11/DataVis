# Required: GitHub repository that holds datasets
GITHUB_OWNER=your-username-or-org
GITHUB_REPO=your-datasets-repo
# Folder path inside the repo (no leading/trailing slashes)
GITHUB_DATA_PATH=datasets

# Optional: PAT for higher rate limits and private repos (repo scope read-only)
# GITHUB_TOKEN=

PORT=3000
